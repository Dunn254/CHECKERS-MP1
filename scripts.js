//create board element(create a div element)
const board = document.createElement("div");
//set the ID
board.id = "checkers-board";
//add the class board
board.classList.add("board")
//create squares using loop(not sure to use for or while...talk to Omar)
for (let x = 1; x <=64; x++) { ... }
